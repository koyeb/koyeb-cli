{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "koyeb",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/koyeb/koyeb.go",
            "args": "${input:args}"
        }
    ],
    "inputs": [
        {
            "id": "args",
            "type": "promptString",
            "default": "help",
            "description": "CLI arguments"
        },
    ]
}